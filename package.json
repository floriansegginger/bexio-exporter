{
  "name": "bexio-export",
  "version": "1.0.0",
  "description": "Export everything from a bexio account (contacts, sales documents with PDFs, purchase bills and receipts, accounting, banking, files, projects, payroll) to JSON, CSV and files.",
  "type": "module",
  "bin": {
    "bexio-export": "./bin/bexio-export.js"
  },
  "scripts": {
    "start": "node bin/bexio-export.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "private": true
}
